trigger:
  batch: true
  paths:
    include:
      - packages/rating-scales/*

pr: none

pool:
  vmImage: "ubuntu-latest"

steps:
  - template: templates/js-pacakge.build.yml
    parameters:
      packageName: "@decsys/rating-scales"
