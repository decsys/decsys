import React from "react";
import { Button } from "@smooth-ui/core-sc";

const EditorBarButton = props => (
  <Button borderRadius={0} width="120px" variant="secondary" {...props} />
);

export default EditorBarButton;
