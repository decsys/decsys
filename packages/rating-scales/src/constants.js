export const labelDistance = "2.8rem";

/**
 * The class name used on the ScaleBar's containing element.
 * May be of interest to anything that wants to select it.
 */
export const ClassName = "js--scalebar"; // TODO: might not need this now, using refs instead?
